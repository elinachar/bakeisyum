<% content_for :title, "Create New User - Bake is Yum" %>
<div class="container user-card text-center">
  <h1>New User</h1>

  <%= render 'form', user: @user %>

  <%= link_to 'Show all users', users_path, class: 'btn btn-default' %>
</div>
